int mul(int x, int y){
    return x * y;
    int b = 10;
}
/*
* My program
*/
int main(){
    int x = 1;
    if (0){}
    while (x < 10) {
    int result = mul(x, 2);
        if ( x > 5) {
            int b = 22;
            break;
            printf(result);
        }
        printf(result); //show the result
        x = x + 1;
    }
    return 0;
}